# project_v0.2

- First validated Python release artifact.
- Fixed a path-traversal defect found by the unit test suite.
- Python bytecode compilation passed.
- Six unit tests passed.

The archive naming convention is now `project_vX.Y.zip`; each new revision must be compiled and tested before it is released.
